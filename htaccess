Options -MultiViews
RewriteEngine On
# Redirigir cualquier petición no encontrada a index.html
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.html [L,QSA]
